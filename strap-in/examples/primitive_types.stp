using @Console; # This is the import of the native Console module
Console c = new Console();


c.out(primitiveOf("This is a String")); # "String"

c.out(primitiveOf('z')); # "Char"

c.out(primitiveOf(42)); # "Int"

c.out(primitiveOf(42.01)); # "Float"

c.out(primitiveOf(True)); # "Bool"

c.out(primitiveOf(None)); # "None"

c.out(primitiveOf(<"Foobar": "Bazboo">)); # "Structure"

c.out(primitiveOf(["Foo", "Bar", "Baz", "Boo"])); # "List"