
# This class only greets users
export class Greeter {
    + String user;

    + Greeter (String user) {
        this.user = user;
    }

    + greetUser :String () {
        String greetings = "Greetings " + this.user + "!";

        >> greetings;
    }
}