using @Console;
using "greeter.stp";

# Each program should have the class "Main" with minimum a public method "main" (which is the entry point)
class Main < Console {

    + !String greet = "foo";
    - Greeter greeter;

    # This method is the program entry point ('+' means that is a public method)
    + main:Int () {
        !String hello = "Hello, World!";
        this.greeter = new Greeter("Tripes");
        this.out(this.greeter.greetUser("Tripes"));
        >> 0;
    }

    #{ This should be a block comment but it isn't parsed
    - someMethod:void () {

    }
    #}

}